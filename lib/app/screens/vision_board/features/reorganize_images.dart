import 'package:flutter/material.dart';

class ReorganizeImage extends StatelessWidget {
  const ReorganizeImage({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return IconButton(
        onPressed: () {},
        icon: const Icon(
          Icons.change_circle,
          color: Colors.blueGrey,
        ));
  }
}
